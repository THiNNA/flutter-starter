/// Advanced feature toggles for A/B testing and phased rollouts.
class FeatureFlags {
  static const bool enableNewProfileUI = false;
  static const bool enablePushNotifications = true;
  static const bool useMockApi = false;
  
  // Future proofing: Fetch these from Remote Config if needed.
}
